# Translated Text - Final 22-6-2021.pdf

Theory  
 
 1. Taking into account that a Keep alive is a communication mechanism that informs a 
process (P) the normal functioning of a second process (H) and thus, in the event that the 
process H stops for a longer time than expected, process P kills it and then executes 
again a possessed H and considering that our process P will only be aware of this, 
asks:  
 a) Propose what communication mechanisms you would use  
 b) Propose a sequence of the mechanism (can be supported by a diagram)  
 c) What functions of C would be involved yes or yes in process P.  
 2. It is requested:  
 a) Complete the following count of positive numbers done in hexadecimal:   
 
 b) If you work with numbers signed using 2's complement, indicate which decimal number 
represents 8A29h in:  
 A 16-bit system:  
 A 32-bit system:  
 3. Differences and relationship between:  
 a) Source, object, binary file.  
 b) Program, process  
 c) In socket: Process, port and IP  
 
 
 
 1. There is a set of functions written in C with the following prototype:  
float FunName(float x);  
Each of these functions takes the value of x, applies a mathematical function to it and returns the 
corresponding ordinate value.  
It is requested:  
 a) Perform a function, perform the composition of two mathematical functions, for this you will be asked 
6-22-2021  
will pass two pointers to functions (type FunNom) that, depending on the order, will correspond to the 
requested composite function. When entering the function, the values of 
Xmin, Xmax and number of points to calculate. The function returns an array of two 
dimensions where in the first column are the abscissa values and in the second are the 
of corresponding ordinates as obtained from the composite function.  
 b) Define the prototype of the function requested in point a. Then give a brief description 
of its use (how to pass the arguments and how to access the data it generates from 
the code that calls it)  
 
 2. Write a code that receives the name of a written source file through main arguments 
In C language, our program has to generate a copy of the entered source file but without 
the comments on their lines.