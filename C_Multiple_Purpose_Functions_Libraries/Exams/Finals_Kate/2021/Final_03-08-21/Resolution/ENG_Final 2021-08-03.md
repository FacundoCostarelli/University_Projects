# Translated Text - Final 2021-08-03.pdf

National Technological University 
Buenos Aires Regional Faculty 
Department of Electronic Engineering
Computer Science I 
0 3 - A G O - 2 0 2 1
Final
Surname and First Names
File Number
Professor
General Rules
Read each question carefully and ask any questions.
of interpretation that could arise.
Theoretical Section
(estimated time 30 minutes):
Point
1)
Yes
the
machine
if
call
INFO-UX
,
the
date
of the
team
is
15-MAY-2021
,
the
user
connected 
in
the
team
is
Louis
Aceto
(ID
of
user
LACETO
)
and
the
directory
in
the
that
if
find
in
that 
moment
is
INFO_1/Class2
inside
of the
directory
staff
,
which
is
the
prompt
that
will show
in
the 
console?
(If
consider
the
prompt
that
configure
the
System
Operational
by
default).
Select
one 
option:
A.
INFO-UX:/home/LACETO/INFO_1/Class2$ 
b.
Luis.Aceto@INFO-UX:/home/Luis.Aceto/INFO_1/Class2$ 
c.
Luis.Aceto@INFO-UX:~/INFO_1/Class2$ 
d.
LACETO@INFO-UX:~/INFO_1/Class2$ 
e.
2021-05-15@INFO-UX:~/INFO_1/Class2$ 
f.
INFO-UX@LACETO:~/INFO_1/Class2$ 
g.
None is correct.
Point 2) Indicate which of the following statements
are true: 
A.
Linux
drive
the
devices
like
yes
were
a
file
and
the
manager
of
do it
for
each 
device is the device driver or controller of the
device. 
b.
one
disadvantage
of the
method
of
complement
to
1
for
the
representation
of
numbers 
signed is that I have a double zero. 
c.
all
the
processes
they have
a
alone
process
father
and
can
be
parents
of
a
alone
process 
children
to
the
time.
Yes
I want
have
a
second
process
son,
i must
wait
to
that
finish
the 
first with the wait function. 
d.
For
generate
a
executable
to
leave
of
a
file
source
in
c
i must
perform
two
steps.
The 
first is the compilation and the second is the linking
or link. 
e.
For
generate
a
program
in
c
I can
use
a
processor
of
text
like
the
words
yes
me 
I make sure not to put any format as a change of
font size or bold. 
f.
The
files
in
Linux
they drive
three
groups
of
permissions,
the
corresponding
to
owner,
to
a 
group and the rest of the users. 
g.
No
if
can
establish
one
connection
to
through
of
a
socket
yes
both
processes
if
they execute 
on the same machine. 
H.
The
system
of
numbering
is
defined
by
the
quantity
of
symbols
that
uses.
This 
amount is called BASE. 
i.
The
named
pipe
or
FIFO
is
a
method
of
CPI
that
allows
link
two
processes
that
if 
run on the same machine even if they are not related
like father-son. 
J.
The
advantage
of
work
with
the
system
HEXADECIMAL
is
that
the
conversion
since
binary
is 
direct from the grouping of three digits
binary. 
K.
In the case of Linux, the graphical interface can be
change and even delete it. 
L.
The
processes
if
identify
with
a
number,
and
one
time
that
if
assigned
the
number
older,
there is 
You have to restart your computer to return the counter to
0. 
M.
Yes
the
machine
has
the
enough
capacity,
I can
have
two
Systems
Operational 
controlling the hardware simultaneously.
P a g
1

Final Computer I
0 3 - A G O - 2 0 2 1
Surname and First Names
File Number
N.
The Operating System connects with the hardware through system calls. 
Or.
In
one
connection
via
Socket,
when
a
process
server
wants
accept
the
communication
with 
a client, you must use the connect function.
Point 3) What is called Kernel of an operating system?
Linux? Select an option:
A.
To the interface that allows the user to enter commands. 
b.
To the list of external commands that can be executed
by the operating system. 
c.
At
set
of
programs
that
constitute
the
System
Operational
properly
said
and
to
your 
corresponding implementation of System Calls. 
d.
At
set
of
policies
that
define
the
way
in
that
if
store
the
files
in
the
disk 
rigid. 
e.
None is correct.
Note for the theoretical part
: 
If
must
deliver
a
file
of
text
where
indicate
the
point
and
the
or
the
letters
of
the
answers
that 
consider correct. 
To pass the theoretical part you must have a minimum
9 points. 
For
the
points
1
and
3,
answer
correct
sum
one,
answer
incorrect
subtraction
1.
No
answered,
no 
addition or subtraction. 
For
the
point
2
each
answer
indicated
if
take
like
true,
the
no
marked
if
take
like 
false. Each correct answer adds one, each answer
incorrect subtraction 1.
Practical Section
(estimated time 2 hours):
Point 1) Make a function that receives a buffer
of integers and convert them to a string. 
Consider
that
the
whole
are
of
4
bytes
and
the
values
corresponding
to
code
ASCII
are
the
8 
bits
of the
second
byteless
signiﬁcant
,
is
say
the
16
bits
of
older
weight
no
they matter,
the
8
bits 
What follows are the ASCII code and the 8 minor bits
weight doesn't matter
. 
Define
the
prototype
that
consider
more
convenient
knowing
that
must
receive
a
vector
of
whole, 
your
length
and
return
a
string
.
Also
must
have
the
option
of
return
a
code
of
error
in 
in case of not being able to generate the phrase
. 
To test the function you can define the vector: 
int v[4]={147456,227328,221184,198656}; 
which will result in the phrase
“Hello”
.
Objectives
: 
●
Evaluate
how
they get
the
data
important
of the
number
(can
be
by
operators
to
level
of 
bits, by mathematical operation (division) or by pointers). 
●
Evaluate the practice to deﬁne an appropriate prototype. 
●
Evaluate a little the handling of strings. 
●
Evaluate
management
of
codes
of
return
like
return
of
error
(the
function
no
must
print 
nothing on screen) 
●
Evaluate where and how memory requests to store
the phrase.
Point
2)
Perform
one
function
that
receive
a
string
corresponding
to
a
name
of
file,
search
the 
last
point
to
the
right
and
return
a
string
that
correspond
to
name
original,
added
the
phrase 
“_decrypt”
and the extension. 
Example:
receive the name
"text.txt"
and must return
"decrypt_text.txt" 
If the name does not contain a period, the phrase will be added
at the end and there will be no point. 
The prototype of the function must be:
int new_name (char *, char *); 
where
the
first
parameter
is
the
name
of the
file
original,
the
second
is
the
name
of the
file
P a g
2
Final Computer I
0 3 - A G O - 2 0 2 1
Surname and First Names
File Number
new
and
returns
a
OK
or
ERROR
(two
constants
symbolic
that
must
define
like
0
and
1)
yes
it
could 
process or not.
Objectives
: 
●
Evaluate string handling. 
●
Evaluate the use of a prototype as requested. 
●
Evaluate
management
of
codes
of
return
like
return
of
error
(the
function
no
must
print 
nothing on screen) 
●
Evaluate the use of symbolic constants (definition
and application).
Point
3)
Perform
one
function
that
receive
the
name
of
a
file.
The
file
is
of the
type
binary
and 
contains a sequence of 4-byte integers.
Its length is not previously known. 
The
function
must
read
the
file
number
by
number
and
go
saving
the
values
in
a
buffer
of 
whole
that
will be
returned
like
result
of
the
function
to
through
of
one
argument
past
by 
reference. 
The prototype of the function must be:
int read(char *, int **); 
where
the
first
parameter
is
the
name
of the
file,
and
the
second
is
the
place
for
save
the 
address
of
memory
where
begins
the
buffer.
will return
a
number
older
or
same
to
zero
with
the 
quantity
of
element
of the
vector
or
ERR_LEC
(constant
symbolic
that
must
define
like
-1)yes
no
it 
could generate.
Objectives
: 
●
Evaluate
management
of
files
(which
it
open,
verify
yes
had
error
of
opening,
that
it
read 
correctly in its entirety and close it). 
●
Evaluate
the
use
of
memory
dynamic
(which
the
ask
and
verify
that
the
beech
granted,
that
in
the 
calling function frees the requested memory). 
●
Evaluate the use of a prototype as requested. 
●
Evaluate the use of passing a pointer by reference. 
●
Evaluate
management
of
codes
of
return
like
return
of
error
(the
function
no
must
print 
nothing on screen) 
●
Evaluate the use of symbolic constants (definition
and application).
Point 4) Make a program that receives by argument
from main one or more file names. 
By
each
name
must
generate
a
process
son
that
if
will order
of
open it
and
load it
in
a
buffer 
using the function from point 3. 
Each
process
must
convert
the
content
of the
buffer
to
text
using
the
function
of the
point
1
and 
generate
a
new
file
(from
text)
with
the
phrase
converted,
whose
name
be
the
generated
in
the 
function 2. 
For
try
the
function
main,
yes
no
has
developed
the
function
of the
point
1,
can
use
a
string 
any in replacement of the function. 
For
try
the
function
main,
yes
no
has
developed
the
function
of the
point
2,
can
use
a
name 
of any file instead of the function. 
You must
validate
all
the
errors
possible
presenting
by
screen
the
message
informative 
corresponding if any occurs. 
The
process
father,
to
measure
that
wow
shooting
the
children,
must
go
completing
a
vector
of 
structures
with
the
information
of
each
process.
The
size
maximum
of
this
vector
will be
of
10 
elements. 
Once you shot all the children and completed the
vector, it will be on hold.will end
when
have
finished
all
the
children
making sure
that
have
concluded 
correctly.
P a g
3
Final Computer I
0 3 - A G O - 2 0 2 1
Surname and First Names
File Number
The structure that will be used to store the information will be:
struct children 
{ 
int pid;
//Number of the generated child process. 
char * file;
//Name of the file used
said process. 
int arg_no;
//Order number in which it was loaded
the name on the command line. 
};
Objectives
: 
●
Evaluate
management
of
arguments
of
main
(validation
that
pass
by
it
less
one,
management
of 
the strings of each one. It's good if they also give
Note that it cannot be more than 10). 
●
Evaluate
management
of
fork
identifying
who
is
the
father,
the
son,
the
use
of
wait
for
no
leave 
zombies or orphans. 
●
Evaluate
management
of
structures.
Definition
of the
array
and
the
load
of
data
in
each
one
of
the 
fields.
Order
of
memory
for
the
names
of
the
files.
Liberation
of
the
memory. 
Memory allocation validation. 
●
Evaluate the use of the return value of functions. 
●
The
attention
of
the
completion
of
the
children
can
be realized
so much
by
interception
of
the
signal 
sigchild as by wait in the main ﬂow. In both
cases may be considered good. 
●
Evaluate if they alone are encouraged to make a child() function
or they leave it all in a kilometer main.
Note for the practical part
: 
You must
deliver
three
files
sources.
The
main.c
where
only
const
the
function
main.
The
functions.c 
where
if
find
the
rest
of
the
functions
requests
and
the
functions.h
where
are
the
prototypes
and 
all additional information that you consider appropriate. 
Remember to apply good programming practices
for the use of files and dynamic memory. 
The assessment of points 1, 3 and 4 is 3 points
each and 1 point for item 2. 
Furthermore
of
reach
the
threshold
of
6
points
must
have
2
functions
good
resolved
and
1
good
raised 
or 1 well resolved and 3 well planned.
P a g
4