/**
 * @file    Respuestas_teoricas.c
 * @brief   [ESP] Respuestas teóricas del 1er Parcial 2019.
 *          [ENG] Theoretical answers for the 1st Midterm 2019.
 *
 * @author  Facundo Costarelli
 * @date    2019
 * @course  Informática 1 — UTNBA
 */

/* 
 * [ESP] Estas respuestas fueron escritas originalmente como texto plano en un .c
 * [ENG] These answers were originally written as plain text in a .c 
 *
 * 1) Que es un sistema operativo?? Que tiene a su cargo??
 *    [ENG] What is an operating system? What is it in charge of?
 *
 * 2) Cual es la diferencia entre un header y una biblioteca?? Que contiene cada uno??
 *    [ENG] What is the difference between a header and a library? What does each contain?
 *
 * 3) ver en el pdf
 *    [ENG] see in the pdf
 *
 * 4) ver en el pdf
 *    [ENG] see in the pdf
 *
 * 1) [ESP] Un SO es una colección de programas que administran recursos (interfaz, memoria, procesador).
 *    [ENG] An OS is a collection of programs that manage resources (interface, memory, processor).
 *
 * 2) [ESP] Una biblioteca contiene código compilado reutilizable. Los headers (.h) contienen prototipos.
 *    [ENG] A library contains reusable compiled code. Headers (.h) contain prototypes.
 *
 * 3) [ESP] sizeof(int *) = sizeof(float *). El tamaño depende de la arquitectura (32bits=4B, 64bits=8B).
 *    [ENG] sizeof(int *) = sizeof(float *). Size depends on architecture (32bits=4B, 64bits=8B).
 *
 * 4) [ESP] Explicación del código sobre punteros desplazándose a posiciones fuera de rango.
 *    [ENG] Explanation of the pointer code shifting to out-of-bounds positions.
 */
