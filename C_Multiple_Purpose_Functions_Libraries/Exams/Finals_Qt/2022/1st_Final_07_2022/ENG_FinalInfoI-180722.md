# Translated Text - FinalInfoI-180722.pdf

National Technological University
Buenos Aires Regional Faculty
Department of Electronic Engineering
Surname and First Name File 
  
 
 
T1 T2 T3 P1 P2 
15% 15% 15% 30% 25% 
     
Approval condition 60%. 
Theoretical:  
1. According to the following statements:
char string[10]="Info 1";
char *p=string;  
 
Indicate the output of the following lines on a 64-bit architecture.
      printf("%li\ n",sizeof(string));
      printf("%li\ n",strlen(string));
      printf("%li\ n",sizeof(p));
      printf("%li\ n",strlen(p));
      printf("%li\ n",sizeof(*p));
2. Differentiate a Union from a Structure. Give an example of each, 
indicating size, declaration, definition, and ways to access its 
members (with their names and with a 
3. Obtain in hexadecimal and decimal A75Dh minus F363h from in:
a. Hexadecimal number system (add mathematically)
b. 2's complement data size 16bits.
c. 2's complement data size 32bits.
Practice:  
1. Based on the Maclaurin series of the cosine function, it can be 
approximate with ௞
௜ୀ଴
It is requested to create a function in C language that generates the coordinates 
of n points (x,y) such that y=cos(x) from a minimum x to a x 
maximum with the following prototype:
int FunCos(double ***points, double Xmin, double Xmax);
The function receives the address of a pointer to an array to save 
the abscissa and ordinate values of each point;  the minimum value and 
maximum of the interval and the number of ter
inside the function, , the number of points you want is requested 
generate and the value of k, returned the number of points 
2. Make a function that returns a pointer to a vector with the 
number of times each letter of the aNational Technological University appears  
Buenos Aires Regional Faculty  
Department of Electronic Engineering  
According to the following statements:  
char string[10]="Info 1";  
 
Indicate the output of the following lines on a 64-bit architecture.
n",sizeof(string));  
n",strlen(string));  
n",sizeof(p));  
n",strlen(p));  
n",sizeof(*p));  
Differentiate a Union from a Structure. Give an example of each, 
indicating size, declaration, definition, and ways to access its 
members (with their names and with a pointer to them)
Obtain in hexadecimal and decimal A75Dh minus F363h from in:
Hexadecimal number system (add mathematically)
2's complement data size 16bits.  
2's complement data size 32bits.  
Based on the Maclaurin series of the cosine function, it can be 
଴௜௫మ೔
(ଶ௜)! for everything x.  
It is requested to create a function in C language that generates the coordinates 
of n points (x,y) such that y=cos(x) from a minimum x to a x 
following prototype:  
int FunCos(double ***points, double Xmin, double Xmax);
The function receives the address of a pointer to an array to save 
the abscissa and ordinate values of each point;  the minimum value and 
maximum of the interval and the number of terms in the series; then, 
inside the function, , the number of points you want is requested 
generate and the value of k, returned the number of points 
Perform a function that returns a pointer to a vector with the 
number of times each letter of the alphabet appears (without ñ and without Computer Science I  
___-___-___ 
 Computer Science I 
  
Note: 
 
Indicate the output of the following lines on a 64-bit architecture.  
Differentiate a Union from a Structure. Give an example of each, 
indicating size, declaration, definition, and ways to access its 
pointer to them)  
Obtain in hexadecimal and decimal A75Dh minus F363h from in:  
Hexadecimal number system (add mathematically)  
Based on the Maclaurin series of the cosine function, it can be 
It is requested to create a function in C language that generates the coordinates 
of n points (x,y) such that y=cos(x) from a minimum x to a x 
int FunCos(double ***points, double Xmin, double Xmax);  
The function receives the address of a pointer to an array to save 
the abscissa and ordinate values of each point;  the minimum value and 
series minos; then, 
inside the function, , the number of points you want is requested 
generate and the value of k, returned the number of points  
Perform a function that returns a pointer to a vector with the 
alphabet (without ñ and without 
 
National Technological University
Buenos Aires Regional Faculty
Department of Electronic Engineering
Surname and First Name File 
  
 
 
T1 T2 T3 P1 P2 
15% 15% 15% 30% 25% 
     
Approval condition 60%. 
distinguish between uppercase and lowercase letters) in a filetext vo. 
Add a main function that receives as an argument the name of the 
file and calls said function. National Technological University  
Buenos Aires Regional Faculty  
Department of Electronic Engineering  
case sensitive) in a text file. 
Add a main function that receives as an argument the name of the 
file and calls that function.  Computer Science I  
___-___-___ 
 Computer Science I 
  
Note: 
 
case sensitive) in a text file. 
Add a main function that receives as an argument the name of the